============
Installation
============

At the command line::

    $ easy_install django-reactive

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-reactive
    $ pip install django-reactive
